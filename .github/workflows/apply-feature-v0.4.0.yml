name: Apply v0.4.0 feature branch

on:
  pull_request:
    branches:
      - main
    types: [opened, synchronize, reopened]

permissions:
  contents: write

concurrency:
  group: apply-v0.4.0-feature
  cancel-in-progress: true

jobs:
  apply:
    if: github.event.pull_request.head.ref == 'feat/v0.4-background-tab-group'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/v0.4-background-tab-group
          fetch-depth: 0
          show-progress: false

      - name: Check whether implementation is already applied
        id: pending
        run: |
          if [[ -d .v040-chunks ]]; then
            echo 'needed=true' >> "$GITHUB_OUTPUT"
          else
            echo 'needed=false' >> "$GITHUB_OUTPUT"
          fi

      - name: Reconstruct source archive
        if: steps.pending.outputs.needed == 'true'
        run: |
          set -euo pipefail
          cp .github/workflows/auto-release.yml /tmp/auto-release.yml
          cat .v040-chunks/part* > /tmp/gpt-notice-v040.tar.gz.b64
          test "$(wc -c < /tmp/gpt-notice-v040.tar.gz.b64)" = "38020"
          echo '446e77d208c0816afcc5d3cc3af7f2dea90c3e6d29e7ce14d26307900530fca0  /tmp/gpt-notice-v040.tar.gz.b64' | sha256sum -c -
          base64 --decode /tmp/gpt-notice-v040.tar.gz.b64 > /tmp/gpt-notice-v040.tar.gz
          echo 'c707fd679cf7dc3fe8dfced933753e08e2827c8c0c77c2c32ab3457464e3daa5  /tmp/gpt-notice-v040.tar.gz' | sha256sum -c -
          gzip -t /tmp/gpt-notice-v040.tar.gz
          tar -xzf /tmp/gpt-notice-v040.tar.gz -C .
          cp /tmp/auto-release.yml .github/workflows/auto-release.yml

      - name: Validate v0.4.0
        if: steps.pending.outputs.needed == 'true'
        run: |
          set -euo pipefail
          node --check background.js
          node --check background-utils.js
          node --check background-tab-groups.js
          node --check background-monitor-tabs.js
          node --check background-watchdog.js
          node --check background-actions.js
          node --check background-events.js
          node --check content.js
          node --check popup.js
          node tests/static.test.js
          node tests/background.test.js
          python3 -m json.tool manifest.json >/dev/null
          test -s icons/chatgpt.png

      - name: Commit implementation
        if: steps.pending.outputs.needed == 'true'
        run: |
          set -euo pipefail
          rm -rf .v040-chunks
          rm -f .v040-trigger .v040-trigger-r5 .v040-diagnostic.txt
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add manifest.json background.js background-utils.js background-tab-groups.js background-monitor-tabs.js background-watchdog.js background-actions.js background-events.js content.js popup.html popup.css popup.js README.md PRIVACY.md tests .v040-chunks .v040-trigger .v040-trigger-r5 .v040-diagnostic.txt
          git commit -m 'feat: replace popup monitor window with background tab group'
          git fetch origin feat/v0.4-background-tab-group
          git rebase origin/feat/v0.4-background-tab-group
          git push origin HEAD:feat/v0.4-background-tab-group
