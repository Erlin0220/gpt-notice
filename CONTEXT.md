# ChatGPT Task Notice

本项目增强仍然打开的 ChatGPT 工作页面，为一次回复提供任务监控，并为后续文本提供安全的本地消息队列与诊断能力。

## 页面与适配

**工作页面**：
扩展支持任务监控或消息队列操作的 ChatGPT 页面。
_Avoid_：聊天页、可用页面

**草稿页面**：
尚未绑定正式会话 ID，但可以发送首条消息的工作页面，包括普通首页和项目主页。
_Avoid_：新建页、空会话

**临时会话**：
已经存在会话内容，但仍使用 `WEB:` 临时标识的会话。
_Avoid_：草稿会话、假会话

**正式会话**：
已经绑定稳定 `/c/<conversation-id>` 路由的会话。
_Avoid_：真实会话、普通会话

**页面实例**：
一次完整文档生命周期；完整刷新会创建新页面实例，SPA 跳转不会。
_Avoid_：标签页、会话实例

**页面状态快照**：
对某一时刻页面可观测事实的结构化只读描述，不包含完整聊天正文。
_Avoid_：任务状态、页面状态机

**页面适配评估**：
根据页面状态快照判断页面支持范围、兼容性等级和安全能力的结果。
_Avoid_：健康快照、任务判断

**兼容性暂停**：
由于页面事实无法安全判断而进入、并且必须由用户明确恢复的队列暂停。
_Avoid_：自动暂停、错误暂停

## 任务与队列

**任务**：
一次用户消息及其对应 ChatGPT 回复的监控生命周期。
_Avoid_：队列任务、作业

**队列项**：
按加入顺序等待发送、发送中或已经完成的一条后续文本消息。
_Avoid_：任务、指令

**会话执行租约**：
保证同一会话同一时刻只有一个页面实例可以自动派发消息的临时所有权。
_Avoid_：页面锁、队列锁

## 诊断

**诊断事件**：
不含聊天正文的重要状态变化或操作结果记录。
_Avoid_：日志、遥测

**诊断报告**：
基于公开诊断模型生成的本地脱敏 Markdown 或 JSON。
_Avoid_：日志导出、聊天导出
