# Changelog

## 0.6.4 - 2026-07-25

- 将新助手回复中的 `data-testid="copy-turn-action-button"` 作为高可信完成信号，出现并稳定 600ms 后可完成任务。
- 保留“新回复正文出现并稳定 4 秒”的兜底，兼容图片、附件、特殊组件和没有复制按钮的回复。
- 发送前记录复制回复按钮数量，避免历史回复的旧按钮导致刚发送就误判完成。
- 项目路径与普通路径只要 `/c/<会话ID>` 相同，就继续监控，不再比较完整 pathname 后误停。
- 页面休眠恢复时短暂进入首页或临时路由，会等待 2 秒确认；普通 Chrome Tab 切换不再停止任务。
- 停止原因改为“已明确进入其他 ChatGPT 会话”，只在新旧会话 ID 确实不同后触发。

## 0.6.3 - 2026-07-25

- 修复新版 ChatGPT 使用 `#composer-submit-button` 时，队列无法识别发送按钮的问题。
- 消息写入后最多等待 2.5 秒，允许 React 完成按钮渲染和启用，不再依赖固定 120ms 延迟。
- 区分输入框缺失、写入失败、按钮缺失和按钮禁用，避免所有故障都误报为“未找到发送按钮”。
- 页面结构或输入状态导致的不可重试故障会暂停队列并恢复原草稿，避免循环重试和连续覆盖。
- 增加 5 秒内相同内容的快速重复入队保护，避免页面状态回弹或连续点击产生重复任务。
- 修复队列有待执行项或覆盖确认时，状态栏仍显示“没有正在进行的会话”的冲突文案。
- 任务提醒模块同步识别新版发送按钮，保证点击发送后的任务建档逻辑继续生效。

## 0.6.2 - 2026-07-25

- 修复新建聊天从首页或项目草稿页自动跳转到正式 `/c/<会话ID>` 时，被误判为切换会话并清空首条任务状态的问题。
- 增加限时 URL 提升机制，仅允许活动任务从草稿页安全绑定到首次生成的正式会话 URL。
- 修复 `PAGE_READY` 仅按 `tabId` 恢复任务，可能把旧会话任务错误绑定到同标签新会话的问题。
- 标签 URL 更新、任务开始、状态上报和心跳增加会话一致性校验；不匹配时安全取消旧任务。
- 修复队列发送确认与手动状态纠偏的竞态，并保留自动覆盖确认的严格派发条件。
- 增加首页到正式会话迁移、同标签跨会话重绑定防护的回归测试。

## 0.6.1 - 2026-07-24

- 修复任务开始和完成状态在消息通道短暂失败时丢失的问题，改为确认成功后提交并有限重试。
- 修复页面刷新恢复缺少助手哈希基线、可能误判完成的问题。
- 修复同一标签切换 ChatGPT 会话后旧任务继续监控新页面的问题。
- 修复队列索引跨标签并发写入可能互相覆盖的问题，优先使用 Web Locks 并提供 Storage 锁回退。
- 修复旧版队列迁移后残留数据可能重新复活的问题。
- 队列正文按 revision 缓存，不再每次状态检查都读取全部长文本。
- 加入队列时验证输入框清空结果，失败会回滚，避免重复入队。
- 修正隐私说明，新增合并前 CI，并让 Release 始终先执行验证。

## 0.6.0 - 2026-07-24

- 移除后台监控标签、GPT 后台标签组、Alarm 看门狗和自动恢复逻辑。
- 改为只监控当前仍然打开的 ChatGPT 页面，页面关闭或离开 ChatGPT 后立即停止监控。
- 修复任务已经结束但缓存状态未更新时“立即执行”仍被阻止的问题。
- 立即执行和队列完成判断改为优先使用停止按钮、忙碌状态、输入框可用性等实时页面信号。
- 修复点击编辑长消息时使用 `execCommand` 和高频 MutationObserver 导致浏览器卡死的问题。
- 输入框内容改为单次原子写入，写入期间暂停相关观察器，并且失败时恢复原草稿。
- 队列列表只渲染 240 字预览和字符数，避免长消息反复参与完整 DOM 重绘。
- 队列正文与索引拆分到独立 Storage 项，加入长内容时不再重写所有历史正文。
- 页面关闭或刷新中断队列任务后自动暂停，避免重新打开页面时重复发送。
- 扩展权限移除 `alarms` 和 `tabGroups`。

## 0.5.4 - 2026-07-23

- 修复队列项编辑、删除、上移和下移按钮因界面持续重绘而无响应的问题。
- 编辑队列消息时改为取回到 ChatGPT 输入框；输入框已有内容时先确认是否覆盖。
- 新增“立即执行”按钮，可覆盖当前输入框内容并直接发送，覆盖前会进行确认。
- 自动执行下一条队列任务时，如果输入框存在草稿，会暂停并询问是否覆盖执行。
- 增加写入失败、发送失败和队列状态变化时恢复原草稿的保护。
- 补充队列交互与确认流程的静态回归测试。

## 0.5.3 - 2026-07-23

- 修复扩展或脚本重载后旧队列面板残留，导致编辑、删除、排序按钮点击无反应。
- 队列 UI 增加脚本实例所有权，检测到失效节点时自动重建并重新绑定事件。
- 修复页面刷新后只依赖停止按钮判断状态，无法识别后台仍在运行的会话。
- 复用任务提醒模块恢复的 `running` / `waiting_action` 状态，阻止误完成和提前发送下一条。
- 队列操作失败时在面板中显示错误提示，不再静默无反应。

## 0.5.2 - 2026-07-23

- 修复项目主页切换到正式项目会话时，旧 DOM 会话 ID 覆盖新 URL 的问题。
- 页面切换时保留旧会话 URL，确保未完成队列由正确的后台标签接管。
- 输入框内容变化后立即刷新“加入队列”按钮状态。
- 复用已有会话标签时防止 Chrome 内存节省挂起；结束后恢复默认状态，不静音、不关闭用户标签。
- 增加项目会话路由、后台队列接管、完成清理和空闲禁入回归测试。

## 0.2.0 - 2026-07-23

- 修复最小化窗口无法判定任务完成。
- 任务存储从单一 `tabId` 升级为多标签绑定。
- 同一会话的运行中任务自动去重。
- 打开任务时复用现有会话标签。
- 避免页面初次加载历史消息时创建重复任务。
- 后台监控页增加周期刷新。
- 自动迁移 v0.1.0 的本地任务数据。

## 0.1.0 - 2026-07-23

- 初始版本。
