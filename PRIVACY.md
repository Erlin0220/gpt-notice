# 隐私说明

ChatGPT 任务完成提醒不收集、出售或上传用户数据。

扩展仅在用户当前打开的 ChatGPT 页面中运行，并在本机执行以下操作：

- 读取页面中的生成状态、确认按钮、错误提示、问题标题、思考时间和回复第一行，用于判断任务状态及生成通知。
- 在 `chrome.storage.local` 中保存扩展设置、任务状态、会话地址和用户主动加入的消息队列。
- 使用 Chrome Notifications API 显示系统通知。
- 用户点击“打开任务”或“打开 ChatGPT”时，聚焦已有标签；只有不存在可用页面且用户主动点击打开时才创建普通标签。

扩展不会：

- 在用户关闭 ChatGPT 页面后继续监控任务。
- 自动创建后台标签、折叠标签组或隐藏窗口。
- 使用定时 Alarm 恢复已关闭页面。
- 调用 ChatGPT 私有接口或读取登录 Cookie。
- 将聊天内容、队列内容或使用数据发送到第三方服务器。

关闭对应 ChatGPT 页面后，页面任务监控立即停止；完全退出 Chrome 后扩展停止运行。
