# 隐私与本地数据

本扩展没有后端、遥测、OpenAI API Key 或云同步，不主动调用 ChatGPT 内部接口。

只有用户主动加入 Queue 的纯文本会保存为待发消息。原生发送事件仅作观察；为识别首页首条消息的路由过渡，会在内存中短暂保留对应文本。已发送队列文本在收到可靠回执后移出待发列表，保留有限的消息标识用于防重。原生响应正文只用于当前页面的完成稳定性判断，不作为聊天历史存储或上传。

本地存储还包含账号/工作区标识的 SHA-256 摘要、对话 URL、发送状态、有限回执/完成标识、观察到的模型用量及手动校正、通知设置与通知跳转信息。不会保存登录令牌、Cookie 内容、邮箱或 API 凭证。

Queue 文本属于浏览器本地明文数据，能访问本机浏览器配置的人可能读取它；不要将本地存储视为加密保险库。卸载扩展可清除扩展存储。旧版本数据仅保留为不执行的备份。

权限用途：`storage` 用于本地持久化，`notifications` 用于系统完成提醒，`tabs` 用于校验当前路由与点击通知后定位正确标签页。仅在 `chatgpt.com` / `chat.openai.com` 页面注入，不监控其他网站。
