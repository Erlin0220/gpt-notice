# 隐私说明

ChatGPT 任务完成提醒不收集、出售或上传用户数据。

扩展仅在用户当前打开的 ChatGPT 页面和本机 Chrome 扩展环境中运行，并执行以下操作：

- 读取页面中的输入框、生成状态、确认按钮、错误提示、消息数量、问题标题、思考时间和回复第一行，用于判断任务状态、运行消息队列和生成通知。
- 在 `chrome.storage.local` 中保存扩展设置、任务状态、会话地址、用户主动加入的纯文本消息队列，以及脱敏诊断事件。
- 使用 Chrome Notifications API 显示系统通知。
- 用户点击“打开任务”或“打开 ChatGPT”时，聚焦已有标签；只有不存在可用页面且用户主动点击打开时才创建普通标签。

## 本地诊断

v0.7.0 提供本地诊断中心，用于定位 ChatGPT 页面结构变化、任务状态、队列暂停、租约和扩展通信问题。

诊断数据：

- 只保存在本机 `chrome.storage.local`。
- 最多保留 200 条事件，并清理超过 7 天的数据。
- 采用允许列表构造，不直接序列化任务、队列或页面内部对象。
- 使用本地随机盐对会话标识生成不可逆短标识。
- 默认不包含完整问题、回复、队列正文、Cookie、Token、完整 URL、原始会话 ID、真实 Tab ID、设备名、本地用户名或文件路径。
- 只有用户主动点击时，才复制 Markdown 摘要或下载 JSON 文件。
- “清除诊断记录”只删除诊断事件和最近页面快照，不删除设置、任务、队列或本地诊断盐。

## 扩展不会

- 在用户关闭 ChatGPT 页面后继续监控任务。
- 自动创建后台标签、折叠标签组或隐藏窗口。
- 使用定时 Alarm 恢复已关闭页面。
- 调用 ChatGPT 私有接口或读取登录 Cookie。
- 将聊天内容、队列内容、诊断数据或使用数据发送到第三方服务器。
- 在页面兼容性受阻时强制自动发送、覆盖草稿、刷新页面或重新打开页面。

关闭对应 ChatGPT 页面后，页面任务监控立即停止；完全退出 Chrome 后扩展停止运行。
