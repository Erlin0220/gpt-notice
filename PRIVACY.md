# 隐私与本地数据

本扩展没有后端、遥测、OpenAI API Key 或云同步，不主动调用 ChatGPT 内部接口。

只有用户主动加入 Queue 的纯文本会保存为待发消息。原生发送事件仅作观察；首页首条消息的路由过渡只保留时间与先前消息标识，不额外保存原生草稿正文。已发送队列文本在收到可靠回执后移出待发列表，保留有限的消息标识用于防重。原生响应正文只用于当前页面的完成稳定性判断，不作为聊天历史存储或上传。系统完成通知会把当前用户问题的短标题、思考耗时和已完成回复的短预览交给 Chrome / 操作系统通知中心显示；这些预览不写入扩展持久化存储，是否保留通知历史由 Chrome / 操作系统设置决定。

本地存储还包含账号/工作区标识的 SHA-256 摘要、对话 URL、发送状态、有限回执/完成标识、观察到的模型用量及手动校正、通知设置与通知跳转信息。不会保存登录令牌、Cookie 内容、邮箱或 API 凭证。

Queue 文本属于浏览器本地明文数据，能访问本机浏览器配置的人可能读取它；不要将本地存储视为加密保险库。卸载扩展可清除扩展存储。旧版本数据仅保留为不执行的备份。

用量观察会在内存中解析允许列表内的 conversation POST 请求体，随即丢弃正文，只在 `storage.session` 临时保留模型、消息 ID、requestId、tab/document 标识、账号摘要及时间。发送事件确认或请求结束后删除关联；异常残留机会性清理，扩展 reload 或浏览器重启也会清空 session。不会读取请求头、响应体或认证凭据。永久本地用量只记录模型、消息 ID、时间与手动配置；刷新时间未知时不会删除仍参与累计的旧 ID。

Popup 与通知跳转均核对当前账号 / Workspace，不按相同对话 URL 跨账号跳转。过期清理只针对无正文、无活动或暂停意图的空闲 Queue bookkeeping；待发和未知结果不会自动清除。

权限用途：`storage` 用于本地持久化与临时关联，`notifications` 用于系统完成提醒，`tabs` 用于校验页面身份与定位正确标签页，`webRequest` 用于只读观察上述原生发送边界。不使用 `webRequestBlocking`、Cookie、debugger、scripting 或无限存储权限。仅在 `chatgpt.com` / `chat.openai.com` 页面注入，不监控其他网站。
